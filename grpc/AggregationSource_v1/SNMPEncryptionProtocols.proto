syntax = "proto3";

package AggregationSource;

enum SNMPEncryptionProtocols {
    SNMPEncryptionProtocols_None = 0;
    SNMPEncryptionProtocols_CBC_DES = 1;
    SNMPEncryptionProtocols_CFB128_AES128 = 2;
}